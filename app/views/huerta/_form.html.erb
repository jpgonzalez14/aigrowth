<%= form_for(huertum) do |f| %>
  <% if huertum.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(huertum.errors.count, "error") %> prohibited this huertum from being saved:</h2>

      <ul>
      <% huertum.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %>
    <%= f.text_field :name %>
  </div>

  <div class="field">
    <%= f.label :type %>
    <%= f.number_field :type %>
  </div>

  <div class="field">
    <%= f.label :user_id %>
    <%= f.number_field :user_id %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
