<%= form_for(microcontrolador) do |f| %>
  <% if microcontrolador.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(microcontrolador.errors.count, "error") %> prohibited this microcontrolador from being saved:</h2>

      <ul>
      <% microcontrolador.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :nombre %>
    <%= f.text_field :nombre %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
